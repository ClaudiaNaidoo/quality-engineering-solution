/**
 * Restful Booker base URL ([API reference](https://restful-booker.herokuapp.com/apidoc/index.html)).
 * Set `API_BASE_URL` in `.env` or the environment (default: production Heroku).
 */
export { getApiBaseUrl } from '../../utils/apiClient.js';
